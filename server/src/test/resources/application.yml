spring:
  datasource:
    url: jdbc:h2:mem:flights_discover;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
    driverClassName: org.h2.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
    open-in-view: false
    hibernate:
      ddl-auto: none